# Candidate release notes — 4.0.0-rc1

This repository publishes the exact `txgraffiti-c3-resolution/4.0.0-rc1`
candidate package supplied on 6 August 2026. The original release files are
preserved without manuscript or claim-text changes.

## Bounded result

The package presents a connected cubic graph on 50 vertices with
`mu_star(G)=15<16=i(G)`, refuting the stated TxGraffiti regular-graph
inequality. It does **not** claim unrestricted order-50 minimality, uniqueness,
secured theorem priority, formal verification, exact-release independent
reproduction, or conventional peer review.

## Assurance boundary

The exact package is an **unrefereed candidate**. Bundled theorem-critical
deterministic checks pass in the recorded environment and from a clean
extracted copy. The exact-release fields in `ASSURANCE.json` remain the
authoritative scope record: independent rerun, independent reimplementation,
formal verification and peer review are false. The conditional restricted
threshold theorem depends on `ZPS-2024-20-CLAUSE`; see
`THEOREM_DEPENDENCY.md`.

## Integrity

The supplied archive SHA-256 is:

```text
94518c6473420d7c048d2381f15aa31f50f802554381883f7be88ad7eb5b331e
```

The internal `MANIFEST.sha256` authenticates the 60 original release files;
the archive and sidecar are also retained under `archives/`.

## Current publisher replay

The publisher built the supplied pinned core container on Apple silicon on
8 August 2026. That current replay is recorded separately in
`REPLAY_CONTAINER_2026-08-08.md`; it does not alter the exact-release
assurance fields or constitute independent reproduction.

## Licensing

The supplied split terms are retained: original prose/data/illustrations are
CC BY 4.0 and original code is MIT, subject to the exclusions in `LICENSE.md`.
Third-party and source-derived material is not relicensed.
